---
- name: Misc
  tags:
    - misc
  block:
    - name: Install tfenv
      git:
        repo: "https://github.com/tfutils/tfenv"
        dest: "~/.tfenv"

    - name: Install istioctlenv
      git:
        repo: "https://github.com/kirecek/istioctlenv"
        dest: "~/.istioctlenv"
